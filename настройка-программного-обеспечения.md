[Назад](/README.md)
### 🪪 Добавление новых карт доступа
Чтобы разрешить доступ нескольким RFID-картам или брелкам, в коде используется специальный список UID-ов — массив разрешённых ключей:

```cpp
const byte authorizedUIDs[][4] = {
    {0x75, 0xBA, 0x7F, 0x05},  // Карта №1
    {0x12, 0x34, 0x56, 0x78}   // Карта №2
    {0xE3, 0x4A, 0x40, 0x4B},  // Карта №3
};
```
___
### 🔍 Как узнать UID своей карты

Залейте [скетч](/Arduino_RFID_Lock.ino) на Arduino.

Откройте Serial Monitor (скорость 9600).

Приложите RFID-карту к считывателю.

В окне монитора появится строка:

```objectivec
Обнаружена карта UID: E4 9A 10 7B

```
Это и есть уникальный идентификатор вашей карты (в шестнадцатеричном формате).
___
### 🧩 Как добавить карту в список

Скопируйте полученный UID и вставьте его в массив:
```cpp
const byte authorizedUIDs[][4] = {
    {0x75, 0xBA, 0x7F, 0x05},  // Карта №1
    {0x12, 0x34, 0x56, 0x78},  // Карта №2
    {0xE3, 0x4A, 0x40, 0x4B},  // Карта №3
    {0xE4, 0x9A, 0x10, 0x7B}// ✅ Новая карта
};
```

Сохраните и перезапишите скетч в Arduino.

Теперь новая карта также откроет замок.
___

### 🧠 Пример вывода UID в Serial Monitor
```text Система готова. Приложите карту...
Обнаружена карта UID: 75 BA 7F 05
✅ Доступ разрешён! Замок открыт на 0.5 секунды.
Замок снова закрыт.
Уберите карту...
Можно прикладывать снова.
```
___
⚙️ Примечания

⚠️ Важно!

Для большинства RFID-карт (MIFARE Classic) UID состоит из 4 байт.
Если у вашей карты UID длиннее (например, 7 байт) — измените размер массива:

```cpp
const byte authorizedUIDs[][7] = { ... };
```

и обновите проверку в функции checkUID().

Никогда не подавайте 5 V на модуль RC522 — используйте 3.3 V!
Перепитка 5 V может повредить микросхему.

Если замок управляется MOSFET-ом или реле, обязательно установите диод (например, 1N4007)![диод](assets/image_6.jpg)  параллельно нагрузке
для защиты Arduino от обратного тока.
___
[Назад](/README.md)